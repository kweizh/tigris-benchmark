{
  "task_name": "cli_full_lifecycle_orchestrator",
  "task_description": "# Tigris CLI: End-to-End Bucket Lifecycle Orchestrator\n\n## Background\nThe Tigris CLI (`@tigrisdata/cli`) exposes every primitive needed to manage a snapshot-enabled bucket: provisioning the bucket with snapshots turned on, uploading objects, taking named snapshots, listing snapshot versions, and tearing the bucket down. This task asks you to chain those primitives together into a single bash script — exactly the kind of recipe that operators use to bootstrap, exercise, and clean up ephemeral storage for an agent run.\n\nSnapshots MUST be enabled at bucket-creation time (the `--enable-snapshots` flag); they cannot be added to an existing bucket. The flow below is sequential — each step depends on the previous one — and the script must clean up after itself by deleting the bucket at the end.\n\n## Requirements\nWrite a single bash script at `/home/user/lifecycle/run.sh` that, **using ONLY the Tigris CLI (`tigris`)**, performs the following steps in order:\n\n1. Creates a bucket named `lifecycle-demo` with snapshots enabled at creation time.\n2. Uploads the file `/home/user/lifecycle/local.txt` (already present in the project directory) into the bucket as the object key `seed.txt`.\n3. Takes a named snapshot of the bucket called `state-A`.\n4. Lists every snapshot of the bucket and writes the snapshot `version` IDs — one per line — to `/home/user/lifecycle/snapshots.txt`. The file must contain at least one snapshot version line.\n5. Deletes the bucket `lifecycle-demo` as a final cleanup step so subsequent runs are repeatable.\n\nThe script must exit with status `0` on success. The verifier will invoke it as `bash run.sh` from `/home/user/lifecycle`.\n\n## Implementation Guide\n1. Change into `/home/user/lifecycle` and create `run.sh`.\n2. Start the script with `#!/usr/bin/env bash` and `set -euo pipefail` so any failed step aborts the whole pipeline.\n3. The container's login shell sources `/etc/profile.d/tigris-auth.sh`, which maps `TIGRIS_STORAGE_ACCESS_KEY_ID` / `TIGRIS_STORAGE_SECRET_ACCESS_KEY` onto `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` (and `AWS_REGION=auto`). When invoked via `bash run.sh` (a non-login shell) you should explicitly source it at the top of your script or export the AWS-compatible variables yourself before running `tigris` commands.\n4. Use the CLI commands listed below — they are the canonical surface for this workflow:\n   ```bash\n   tigris buckets create lifecycle-demo --enable-snapshots\n   tigris objects put lifecycle-demo seed.txt /home/user/lifecycle/local.txt\n   tigris snapshots take lifecycle-demo state-A\n   tigris snapshots list lifecycle-demo --format json   # parse versions\n   tigris buckets delete lifecycle-demo --yes\n   ```\n5. Parse the `tigris snapshots list ... --format json` output to extract every `version` value (one per line) and write them to `/home/user/lifecycle/snapshots.txt`. `jq` is preinstalled in the environment, e.g.\n   ```bash\n   tigris snapshots list lifecycle-demo --format json \\\n     | jq -r '.snapshots[].version' >/home/user/lifecycle/snapshots.txt\n   ```\n6. Make `run.sh` executable (`chmod +x run.sh`) — although the verifier runs it as `bash run.sh`, leaving the execute bit on is good hygiene.\n\n## Constraints\n- Project path: `/home/user/lifecycle`\n- Script path (fixed): `/home/user/lifecycle/run.sh`\n- Snapshot file path (fixed): `/home/user/lifecycle/snapshots.txt`\n- Bucket name (fixed): `lifecycle-demo`\n- Object key (fixed): `seed.txt`\n- Snapshot name (fixed): `state-A`\n- Source file to upload (fixed, already present on disk): `/home/user/lifecycle/local.txt`\n- The script MUST use only the Tigris CLI (`tigris`) for all bucket / object / snapshot operations — do NOT call raw S3 / HTTP APIs or other SDKs.\n- The script MUST delete the bucket at the end (cleanup). After the script exits, `tigris buckets list` must no longer show `lifecycle-demo`.\n- The script MUST exit with status `0` on success.\n- Snapshots MUST be enabled at creation time using `--enable-snapshots`; enabling snapshots on an existing bucket is not supported.\n\n## Integrations\n- Tigris Object Storage (admin credentials are provided to the environment as `TIGRIS_STORAGE_ACCESS_KEY_ID` and `TIGRIS_STORAGE_SECRET_ACCESS_KEY`).",
  "truth": "# Verification Plan\n\n## Setup\n1. `cd /home/user/lifecycle`\n2. Verify the agent's script exists at `/home/user/lifecycle/run.sh` and that the seed file `/home/user/lifecycle/local.txt` is present.\n3. Pre-delete the bucket `lifecycle-demo` (best-effort) so the script starts from a clean state if a previous attempt left it behind.\n4. The verifier maps `TIGRIS_STORAGE_ACCESS_KEY_ID` / `TIGRIS_STORAGE_SECRET_ACCESS_KEY` onto `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` / `AWS_REGION=auto` for every `tigris` subprocess call.\n\n## Verification Steps\n1. Execute the agent's script with `bash run.sh` from `/home/user/lifecycle`, capturing stdout/stderr. Assert that the script exits with status `0` within a reasonable timeout (e.g. 300 seconds).\n2. Confirm that `/home/user/lifecycle/snapshots.txt` exists and that it contains at least one non-empty line (each line is expected to be a snapshot `version` string).\n3. Confirm via `tigris buckets list --format json` that the bucket `lifecycle-demo` is NOT present after `run.sh` exits — i.e. the script performed its cleanup step.\n\n## Specific Test Data\n- Bucket name: `lifecycle-demo`\n- Snapshot name written to bucket: `state-A`\n- Object key uploaded: `seed.txt`\n- Local source file: `/home/user/lifecycle/local.txt`\n- Snapshot output file: `/home/user/lifecycle/snapshots.txt`\n- Required credentials (injected by Harbor at run time): `TIGRIS_STORAGE_ACCESS_KEY_ID`, `TIGRIS_STORAGE_SECRET_ACCESS_KEY`.\n\n## Expected Results\n- `bash run.sh` exits with status code `0`.\n- `/home/user/lifecycle/snapshots.txt` exists and has at least one non-empty line.\n- `tigris buckets list --format json` does not include any bucket named `lifecycle-demo` after `run.sh` completes (cleanup performed).\n\n## Integrations\n- Tigris Object Storage."
}
